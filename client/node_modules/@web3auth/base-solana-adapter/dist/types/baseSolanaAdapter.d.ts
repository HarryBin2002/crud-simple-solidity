import { BaseAdapter, UserAuthInfo } from "@web3auth/base";
export declare abstract class BaseSolanaAdapter<T> extends BaseAdapter<T> {
    clientId: string;
    constructor(params?: {
        clientId?: string;
    });
    authenticateUser(): Promise<UserAuthInfo>;
    disconnect(): Promise<void>;
}
